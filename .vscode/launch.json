{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/BACKEND-IN-JS/package.json",
                "localRoot": "${workspaceFolder}/BACKEND-IN-JS"
            }
        }
    ]
}