{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "node",
			"dockerBuild": {
				"dockerfile": "${workspaceFolder}/BACKEND-IN-JS/Dockerfile",
				"context": "${workspaceFolder}/BACKEND-IN-JS",
				"pull": true
			},
			"node": {
				"package": "${workspaceFolder}/BACKEND-IN-JS/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: release",
			"dependsOn": [
				"docker-build"
			],
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/BACKEND-IN-JS/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"env": {
					"DEBUG": "*",
					"NODE_ENV": "development"
				}
			},
			"node": {
				"package": "${workspaceFolder}/BACKEND-IN-JS/package.json",
				"enableDebugging": true
			}
		}
	]
}